<div class="container mt-4">
    <h2>Upravit příspěvek</h2>
    
    <form action="index.php?page=article-edit&id=<?= $article->id ?>" method="post">
        <div class="mb-3">
            <label class="form-label">Název</label>
            <input type="text" class="form-control" name="title" value="<?= htmlspecialchars($article->title) ?>" required>
        </div>
        <div class="mb-3">
            <label class="form-label">Abstrakt</label>
            <textarea class="form-control" name="abstract" rows="5" required><?= htmlspecialchars($article->abstract) ?></textarea>
        </div>
        <button type="submit" class="btn btn-primary">Uložit změny</button>
        <a href="index.php?page=my-articles" class="btn btn-secondary">Zrušit</a>
    </form>
</div>