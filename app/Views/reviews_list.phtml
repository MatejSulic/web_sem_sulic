<div class="container mt-4">
    <h2>Moje recenze</h2>

    <?php if (isset($_GET['msg']) && $_GET['msg'] == 'saved'): ?>
        <div class="alert alert-success">Hodnocení uloženo.</div>
    <?php endif; ?>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>Článek</th>
                <th>PDF</th>
                <th>Stav hodnocení</th>
                <th>Akce</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($reviews as $r): ?>
            <tr>
                <td><?= htmlspecialchars($r->title) ?></td>
                <td>
                    <a href="uploads/<?= htmlspecialchars($r->filename) ?>" target="_blank" class="btn btn-sm btn-outline-primary">Stáhnout PDF</a>
                </td>
                <td>
                    <?php if ($r->score_technical > 0): ?>
                        <span class="badge bg-success">Ohodnoceno</span>
                    <?php else: ?>
                        <span class="badge bg-warning text-dark">Čeká na hodnocení</span>
                    <?php endif; ?>
                </td>
                <td>
                    <?php if ($r->article_status == 'pending'): ?>
                        <a href="index.php?page=review-edit&id=<?= $r->id ?>" class="btn btn-sm btn-primary">Hodnotit</a>
                    <?php else: ?>
                        <span class="text-muted">Uzavřeno</span>
                    <?php endif; ?>
                </td>
            </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
</div>